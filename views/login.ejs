<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* Reset some styles for better control */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #ff7f50, #1e3a8a); /* Orange to Blue gradient */
            color: white;
        }

        .container {
            background: rgba(255, 255, 255, 0.9); /* White background with slight transparency */
            padding: 30px 40px;
            border-radius: 10px;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        h2 {
            color: #1e3a8a; /* Dark Blue for heading */
            font-size: 24px;
            margin-bottom: 20px;
            font-weight: 600;
            letter-spacing: 1px;
        }

        input {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            background-color: #f9fafb;
            color: #333; /* Text color for input */
            transition: all 0.3s ease;
        }

        input:focus {
            border-color: #ff7f50; /* Orange color for focus */
            box-shadow: 0 0 10px rgba(255, 127, 80, 0.5); /* Orange glow on focus */
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #1e3a8a; /* Dark Blue button */
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            transition: transform 0.2s ease;
            font-weight: 600;
        }

        button:hover {
            transform: translateY(-5px);
            background-color: #334f87; /* Lighter Dark Blue on hover */
        }

        button:active {
            transform: translateY(2px);
        }

        /* Responsive design for smaller screens */
        @media (max-width: 600px) {
            .container {
                width: 90%;
                padding: 25px 30px;
            }

            h2 {
                font-size: 20px;
            }

            input,
            button {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login</h2>
        <form id="loginForm" action="/todo" method="GET">
            <input type="text" id="username" name="username" placeholder="Username" required>
            <input type="password" id="password" name="password" placeholder="Password" required>
            <button type="submit">Login</button>
        </form>
    </div>

    <script>
        // Optional client-side validation or simulation if needed
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault(); // Prevent form submission for now
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            if (username && password) {
                // Simulate a successful login
                window.location.href = '/todo'; // Redirect to To-Do page
            } else {
                alert('Please enter both username and password');
            }
        });
    </script>
</body>
</html>
